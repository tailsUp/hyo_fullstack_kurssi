let gql;_830‍.x([["typeDefs",()=>typeDefs],["resolvers",()=>resolvers],["default",()=>_830‍.o]]);_830‍.w("apollo-server",[["gql",["gql"],function(v){gql=v}]]);

       const typeDefs = gql`
  extend type Query {
    """
    Returns repository by an id.
    """
    repository(id: ID!): Repository
  }
`;

       const resolvers = {
  Query: {
    repository: async (obj, args, { dataLoaders: { repositoryLoader } }) =>
      repositoryLoader.load(args.id),
  },
};

_830‍.d({
  typeDefs,
  resolvers,
});
