_6af‍.x([["default",()=>_6af‍.o]]);const parseCursor = cursor => {
  if (!cursor) {
    return null;
  }

  try {
    return JSON.parse(Buffer.from(cursor, 'base64').toString('utf8'));
  } catch (e) {
    return null;
  }
};

_6af‍.d(parseCursor);
